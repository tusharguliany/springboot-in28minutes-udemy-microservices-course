spring:
  application:
    name: currency-exchange-service
  cloud:
    config:
      uri: http://localhost:8888
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true    
server:
  port: 8082
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka